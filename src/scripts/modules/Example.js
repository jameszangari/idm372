module.exports = {
    init: function() {
        // Check if element has class name before running script
        if (document.querySelector('.class-name') == null) {
            return;
        }
        // Insert stuff here
    },
}