<% if (typeof form != 'undefined') { %>
    <button class="<%= classnames %>" data-form="<%= form %>" data-href="<%= url %>"><%= label %></button>
<% } else { %>
    <a class="<%= classnames %>" href="<%= url %>"><%= label %></a>
<% } %>